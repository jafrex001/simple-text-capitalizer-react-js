import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="header">
      <h1>Lowercase to Uppercase text converter</h1>
    </header>
  );
};

export default Header;
